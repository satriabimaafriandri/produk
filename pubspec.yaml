name: produk_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0  # Tambahkan ini
  intl: ^0.19.0  # Tambahkan ini untuk format Rupiah
  image_picker: ^1.1.2 # TAMBAHAN BARU: Untuk fitur upload gambar produk dari galeri/kamera

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true